# Markdown Templates — Placeholder

Markdown document templates are not yet included. When the user selects Markdown as the output format, the `/document-prep --new` command will scaffold basic structure from general knowledge.

Contributions welcome: add `.md` templates following the same naming convention as the LaTeX templates.
