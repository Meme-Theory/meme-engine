# Typst Templates — Placeholder

Typst document templates are not yet included. When the user selects Typst as the output format, the `/document-prep --new` command will report this and suggest creating templates manually or using the formatter agent.

Contributions welcome: add `.typ` templates following the same naming convention as the LaTeX templates.
